# AGENTS.md

> 把本文件放到你的**项目仓库根目录**（不是本 MCP 项目的根目录），Codex 会在每次会话开始时读取它。
> 只想加规则、不想覆盖已有 AGENTS.md 的话，把下面 `## 向用户提问` 一节整体追加到你现有文件里即可。

## 向用户提问

你可以通过 `human_input` MCP server 向用户发起结构化提问，并等待回答，体验接近 Claude Code 的 AskUserQuestion。

**只有在「决策价值 > 打断成本」时才提问。** 提问会打断用户，而提问本身也有成本。

### 该提问的情形

- 需求存在**关键歧义**，猜错的代价是返工（不是多打几个字）。
- **架构选择**会明显影响后续实现，且反过来很难改（模块边界、数据格式、并发模型、状态归属）。
- 存在**多个同样合理**的方案，用户偏好才是决定因素。
- 操作涉及**删除、覆盖、迁移、破坏性变更**（`rm -rf`、force push、schema 迁移、批量重写、发布）。
- 用户可见行为存在**真实取舍**（错误处理策略、默认值、破坏性 vs 兼容性、性能 vs 可读性）。

### 不该提问的情形

- 小问题不要频繁打断用户。
- 能安全推断的细节**自行决定**：命名、注释风格、目录内的文件拆分、明显的 import 顺序、符合仓库既有约定的写法。
- 答案能从仓库里读出来（`package.json`、现有代码、README、测试）——**先去读**。
- 答案不会改变你下一步做什么。
- 你已经问过一次并且得到了回答——**不要重复问**。

拿不准时按这个顺序：**先读代码 → 再按仓库既有约定推断 → 仍然影响重大且无法推断时才提问。**

### 工具选择

| 场景 | 工具 |
| --- | --- |
| 2–5 个合理候选，需要用户挑一个 | `ask_choice` |
| 是/否决策，尤其破坏性操作前的门禁 | `ask_confirm` |
| 真正开放的输入（URL、路径、账号标识、额外上下文） | `ask_text` |
| 需要用户选一个**子集**（目标平台、启用的检查项、纳入的模块） | `ask_multi_select` |

### 调用纪律

- **有 2–5 个合理候选时优先 `ask_choice`**，并在 `options[].description` 里写清每个选项的后果，让用户能一眼判断。
- **是/否决策用 `ask_confirm`**，不要在 `question` 里只写「确定吗？」——要写清**具体对象和具体后果**（"删除 `src/legacy/` 下 47 个文件，它们没有被任何入口引用"）。
- **自由输入用 `ask_text`**，仅当答案无法枚举时使用。
- `ask_multi_select` 只有在「子集」语义成立时用；`min`/`max` 只在数量真的重要时才设。
- 破坏性操作前**必须**用 `ask_confirm`，且**不要**预设 `default = true`。

### 处理返回值（重要）

每个 `ask_*` 都返回统一的 `status` 字段。**只有 `status: "answered"` 才代表拿到了答案。**

- `answered` — 用 `answer` / `selected` / `confirmed` / `free_text`，继续干活，不要重复问同一题。
- `needs_user_input` — MCP elicitation 不可用，题目**从未展示给用户**。把 `message` 里的问题和选项**原样**复述给用户，等他们回复后再继续。
- `declined` — 用户拒绝回答。**不要再问**。选最保守的方案、明确说明你的假设，或者停下来报告需要决策。
- `cancelled` / `timeout` — 没有人回答。**未回答绝不等于许可**：破坏性操作上不要猜，选安全默认值或报告阻塞。
- `invalid_response` / `unsupported` / `error` — 阅读 `message` 和 `next_step` 并照做。

**永远不要**因为拿不到答案就默默假定一个值然后继续——尤其在删除、覆盖、迁移类操作上。
